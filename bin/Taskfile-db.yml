version: "3"

tasks:
  start-mongo:
    cmds:
      - echo "Starting MongoDB..."
      - docker run --name mongo -d -p 27017:27017 mongo:{{.MONGO_VERSION}}

  start-redis-single:
    cmds:
      - echo "Starting Redis single instance..."
      - docker run --name redis-single -d -p 6379:6379 redis:{{.REDIS_VERSION}}

  start-redis-cluster:
    cmds:
      - echo "Starting Redis Cluster..."
      - REDIS_VERSION={{.REDIS_VERSION}} docker-compose up -f redis-cluster/docker-compose.yml -d
